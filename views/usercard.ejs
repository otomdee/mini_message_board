<div class="messageCard">
    <span> <%= item.user%> :</span>
    <span> <%= item.text%></span>
    <span> <%= item.added%></span>
    
    <a href=<%= `/message/${item.id}` %>>
        <button>Open Full Message</button>
    </a>
</div>